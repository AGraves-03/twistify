Hello User!
---------------
\\ functions - behaviors
aiCodeReview() - generate useless, no meaning "Review" from "AI".
unpredictableDefineBomb() - create a header file with random #define keyword in it.
randomCodeInjection() - Injects code into your main file.
timeTravelerBug() - sets your clock date back.
stealthyCorruptor() - changes something within your main file.
pseudoError() - produces random "errors".
chaoticLoop() - runs for a specified amount, but might skip a few once or twice.
tangleMultiply(int a, int b) - randomizes the inputed variables and then multiplies them.
tangleSort(std::vector<int>& nums) - sorts a list of items but then randomizes them but then sprts them again.
echoMadness(const std::string& message, int count) - repeats a string for a specified amount of time.
swapception(int& a, int& b) - swaps two variable values in a loop.
reverseString(const std::string& str) - flips the provided string.